# Keep the interaction model in its own document

The current product spec stays compact, `brainstorm.md` remains a historical handoff, and the detailed ScopeX interaction rules live in `docs/INTERACTION_MODEL.md`. The interaction model is large enough to need its own readable document, but rewriting the origin handoff would erase useful design history.
